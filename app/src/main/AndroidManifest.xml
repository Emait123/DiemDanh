<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.theodoiquatrinhhoc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TheoDoiQuaTrinhHoc">
        <activity
            android:name=".TeacherSubjectActivity"
            android:label="Điểm danh"
            android:parentActivityName=".TeacherSubjectListActivity"
            android:exported="false" />
        <activity
            android:name=".StudentSubjectActivity"
            android:label="Điểm danh"
            android:exported="false"
            android:parentActivityName=".StudentSubjectListActivity" />
        <activity
            android:name=".StudentSubjectListActivity"
            android:exported="false"
            android:label="Danh sách môn"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".TeacherSubjectListActivity"
            android:exported="false"
            android:label="Danh sách môn"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".AddSubjectActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Đăng nhập">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>